/* Force-included when building the PC preview: types the CE toolchain has. */
#ifndef HOST_COMPAT_H
#define HOST_COMPAT_H
#include <stdint.h>
typedef uint32_t uint24_t;
typedef int32_t int24_t;
#endif
